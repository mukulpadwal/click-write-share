<%- include("partials/header"); -%>

<h1>Home</h1>
<p><%= startingText %></p>

<!-- <% for(var i=0; i<blogs.length; i++) { %>
    <h1> <%= blogs[i].titleText %> </h1>
    <p> <%= blogs[i].postText %> </p>
<% } %> -->

<% blogs.forEach(function (blog){ %>
    <h1> <%= blog.title %> </h1>
    <p> <%= blog.content.substring(0,101) + " ... "%> <a href="blogs/<%= blog.title %>">Read More</a> </p>
<% }); %>

<%- include("partials/footer"); -%>